{{#partial "content"}}

    <h1> List of Heroes!</h1>

    {{#if heroes}}
        {{#each heroes }}
            <p><a href="/squads/{{categoryId}}/heroes/{{id}}">{{description}}</a></p>
        {{/each}}
    {{else}}
     <p>No heroes!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}